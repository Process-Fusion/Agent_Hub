# Core dependencies
langchain-core>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
langgraph-checkpoint-postgres>=2.0.0
langsmith>=0.1.0

# Environment variables
python-dotenv>=1.0.0

# PDF processing (choose one or both)
pymupdf>=1.23.0      # PyMuPDF - recommended, faster
pdf2image>=1.16.0    # Alternative, requires poppler

# Image processing
Pillow>=10.0.0

# Pydantic for state models
pydantic>=2.0.0

# Web framework and async DB
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0

# Testing
pytest
httpx>=0.20.0        # ASGITransport for controller tests

psycopg2-binary
psycopg[binary]>=3.1.0